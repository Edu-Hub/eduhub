version: "3.7"

services:
  db:
    container_name: mongodb
    image: mongo:4.2
    environment:
      - MONGO_INITDB_DATABASE=eduhub
      - ME_CONFIG_MONGODB_SERVER=mongodb
    ports:
      - '27017:27017'

  mongo-express:
    container_name: mongo-express
    image: mongo-express
    depends_on:
      - db
    environment:
      - ME_CONFIG_MONGODB_SERVER=mongodb
      - ME_CONFIG_BASICAUTH_USERNAME=admin
      - ME_CONFIG_BASICAUTH_PASSWORD=pass
    ports:
      - '8081:8081'
